rootProject.name = "servertils"

pluginManagement {
    repositories {
        gradlePluginPortal() // For Kotlin plugin
        maven("https://maven.fabricmc.net/") // For Fabric Loom
        mavenCentral()
    }
}